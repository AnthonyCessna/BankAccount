# Write your code here
import random

class BankAccount:
    
    account_numbers = []
    
    def __init__(self):
        
        random.seed()
        random_credit_number = "400000" 
        
        self.balance = 0
        
        for i in range(10):
        
            random_credit_number = random_credit_number + str(random.randint(0,9))
            
        self.credit_card_number = random_credit_number
        
        self.pin = ""
        
        for i in range(4):
            
            self.pin = self.pin + str(random.randint(0, 9))
            
        account_numbers.append(self)

while menu_selection != 0:
        
    print("1. Create an account")
    print("2. Log into account")
    print("0. Exit")
                
    global menu_selection = int(input())

    if menu_selection == 1:
        
        new_account = BankAccount()
        
    elif menu_selection == 2:
    
        print("Enter your card number:")
        account_number = input()
        
        for account in account_numbers:
            
            if account.credit_card_number == account_number:
                
                print("Enter your PIN:")
                pin_number = input()